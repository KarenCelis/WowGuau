<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyecto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity android:name=".HomeDueno"></activity>
        <activity
            android:name=".RegistrarMascota"
            android:label="RegistrarMascota"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".HomePaseador"
            android:label="@string/title_activity_home_paseador"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CertificadoPaseador"
            android:label="@string/certificado_paseador"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TipoUsuario"
            android:label="Tipo Usuario"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TerminosCondiciones"
            android:label="@string/term_y_cond_title"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Register"
            android:label="@string/registro"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Home"
            android:label="@string/home"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>